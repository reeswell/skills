# Cursor

For Cursor, copy `.cursor/rules/reeswell.mdc` into the target project's
`.cursor/rules/` directory.

The rule is configured with `alwaysApply: true`, so Cursor should apply it
automatically while editing JavaScript and TypeScript code.
