# Codex

For Codex, install `skills/reeswell` into `~/.codex/skills/reeswell` for
portable skill use, or copy `AGENTS.md` into a project root for project-level
instructions.

Invoke explicitly when helpful:

```text
Use $reeswell to refactor this TypeScript feature while keeping the change small.
```
